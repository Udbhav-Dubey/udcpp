#include <bits/stdc++.h>
using namespace std;
int main (){
    unordered_set<int> s;
    s.insert(1);
    s.insert(11);
    s.insert(121);
    for (auto it : s){
        cout << it << " ";
    }
return 0;
}
